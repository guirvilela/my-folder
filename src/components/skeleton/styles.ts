import { StyleSheet } from "react-native";

export const styled = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    borderRadius: 8,
    marginBottom: 18,
  },
  skeletonContainer: {
    width: 164,
    maxWidth: 164,

    height: 180,
    borderRadius: 8,
  },
});
